# Email Triage MVP — Python 3.11+
# Unpinned so installs resolve cleanly across environments. Pin exact versions
# later (pip freeze > requirements.lock) once you have a working setup you like.
flask
anthropic
google-api-python-client
google-auth
google-auth-oauthlib
cryptography
python-dotenv
